# Absolute URL of the sjosyn-app Netlify backend (separate deploy from the
# web PWA). Copy this file to .env and fill in the real site URL once created.
VITE_API_BASE=https://your-sjosyn-app-site.netlify.app
